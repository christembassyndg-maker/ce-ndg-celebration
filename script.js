const lines=[...document.querySelectorAll('.line')];let i=0;function hide(){curtain.classList.add('hide');document.body.classList.remove('locked')}const timer=setInterval(()=>{lines[i].classList.remove('active');i++;if(i>=lines.length){clearInterval(timer);hide();return}lines[i].classList.add('active')},1850);skipIntro.onclick=()=>{clearInterval(timer);hide()};const audio=document.getElementById('music');audio.volume=0;let on=true;function music(){audio.play().catch(()=>{});let v=0;const f=setInterval(()=>{if(!on){clearInterval(f);return}v+=.025;audio.volume=Math.min(v,.55);if(audio.volume>=.55)clearInterval(f)},120)}musicBtn.onclick=()=>{on=!on;musicBtn.textContent=on?'Music: On':'Music: Off';if(on){audio.volume=.55;audio.play().catch(()=>{})}else audio.pause()};seal.onclick=()=>{music();envelope.classList.add('opened');setTimeout(()=>{opening.style.display='none';invite.style.display='block';setTimeout(()=>invite.classList.add('show'),40)},1500)};function cd(){const e=new Date('2026-07-31T19:00:00-04:00').getTime();let diff=Math.max(0,e-Date.now());let d=Math.floor(diff/86400000);diff-=d*86400000;let h=Math.floor(diff/3600000);diff-=h*3600000;let m=Math.floor(diff/60000);diff-=m*60000;let s=Math.floor(diff/1000);days.textContent=d;hours.textContent=h;minutes.textContent=m;seconds.textContent=s}cd();setInterval(cd,1000);const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));